---
description: AI 驱动开发与开源文档规则
alwaysApply: true
---

# OpenSwitch AI 协作规则

## 目标

确保 OpenSwitch 以「需求 -> 实现 -> 文档」同步推进，保证开源可读性与持续维护性。

## 强制要求

- 任何功能变更都要同步更新相关文档（至少包含 `README.md` 或 `docs/` 中对应文件）。
- 新功能实现前先在 `docs/` 明确需求边界与验收标准，再开始编码。
- 默认优先最小可运行改动，避免一次性引入无验证的大范围重构。
- 所有新增文件与目录命名保持清晰、可预测，避免临时性命名。
- 讨论和实现中出现的新决策，需要记录在文档中，避免只存在于对话上下文。

## 开源可维护性要求

- `README.md` 必须保持可独立理解：项目目标、能力范围、当前状态、下一步计划。
- 对外依赖与系统约束（如 macOS API 限制）需在文档中明确说明。
- 若行为发生变更（命令、配置、目录结构），在同一改动中更新文档。

## 交付前检查清单

- 需求文档是否覆盖本次功能
- 代码与文档是否一致
- README 是否仍能说明项目现状
- 是否留下可执行的下一步任务
